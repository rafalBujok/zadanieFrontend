import { DateMonthYearPipe } from './date-month-year.pipe';

describe('DateMonYearPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMonthYearPipe();
    expect(pipe).toBeTruthy();
  });
});
